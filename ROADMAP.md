# ROADMAP / 運用メモ

最終更新: 2026-07-17。実装の全体像は [ARCHITECTURE.md](ARCHITECTURE.md)、
テスト手順は [README.md](README.md) を参照。

---

## 1. 残タスク

登録済みのタスク(調査/抽出システム/イベント辞書/アドバイザー/使用率DB差し替え)は
すべて完了。以下は実装過程で確認された未対応・既知の粗さ。

### 優先度高 (実運用テストで影響が出やすい)

- [ ] **OBS実映像での通し再検証**: 2026-07-18の初回実戦テストで「イベント/アドバイスが
      一切出ない」問題が発生 → 原因2件を特定・修正済み:
      (1) `RegionStabilizer` の固定クールダウン25フレーム(≒5秒超)が連続メッセージを
      軒並み取りこぼしていた → クールダウン廃止、doneフラグ+内容変化検知に変更
      (2) サーバーが同期処理でイベントループを塞ぎフレームが無制限に滞留
      → executor実行+最新フレーム優先ドロップ+EasyOCR起動時プリロードに変更。
      ストリーム経路のシミュレーション(JPEG圧縮+同一フレーム連続投入)では
      交代/特性/メガシンカ/技イベントの発火を確認済み。**実戦での再テストが必要**。
      サーバーログに5秒ごとの scene/処理数/破棄数が出るので、そこで動作確認できる
- [ ] ゾーンずれが残る場合: `DEBUG_DUMP_FRAMES=1 uvicorn server:app_asgi --port 8000`
      で受信フレームを `debug_frames/` に保存し、
      `python -m tools.run_images debug_frames/` と
      `python -m tools.debug_zones <フレーム> <出力> <scene>` で確認して
      `vision/zones.py` を調整
- [ ] **git commit**: vision/ advisor/ tools/ tests/ ROADMAP.md 等が未コミット

### 2026-07-18 実戦テストでの追加修正 (適用済み)

- OCRバックエンドを **Apple Vision (macOS内蔵)** 主体に変更 (EasyOCRはフォールバック)。
  OBS実映像の小さい文字はEasyOCRでは読めず、Visionで名前6/6・持ち物6/6を達成。
  数字ゾーンは en-US 単独認識 (日本語モードは斜体の「197/197」を誤読するため)
- **OBSは1920x1080必須** (720pでは文字が潰れる)。README修正済み
- タイプアイコン判定を色ベース→**グリフ形状(dHash)+色の複合**に変更。
  実フレームから正解ラベル付きテンプレートを生成 (`tools/make_type_templates.py`、
  保存先 `images/type_templates_real/`)。空スロット (単タイプ) は分散チェックで棄却
- 技リストは選択中の行が拡大されるため固定ゾーンを廃止し、
  **白い縁取り線ペアによる動的行検出** (`scenes.detect_move_pills`) に変更
- バトルHUD判定に自分のHPバー色画素を追加 (リザルト画面をHUDと誤検出していた対策)。
  選出画面は3フレーム連続で確定 (対戦中の1フレーム誤分類で状態を壊さない)

### 2026-07-18 「場の状況」画面対応 (適用済み)

- **場の状況確認画面の検出・抽出を実装** (`extract_field_check`)。実機フレーム+Web調査
  (jara-blog.com/rank のSwitch版スクショ解析) に基づく:
  - 検出: 画面中央の藍紫(自分)/深紅(相手)大パネル + アンカー文言「効果と場の状態」OCR
  - 抽出: 表示中ポケモン (パネル色で陣営判定)・HP・タイプ・**ランク倍率「×n.n」→段階**
    (7ステータス、行OCRの座標でアンカー)・**場の効果「〜状態 n/m」→残りターン**
    (天候/フィールド/壁/設置技/状態異常)
  - 実フレームで検証済み: カバルドン71%/じめん/ランク±0/すなあらし残り3T
  - アドバイスの📱提案がランク変化検知後にこの画面を開くよう促す
  - ⚠ ランクが実際に変化した状態 (▲▼あり) のフレームは未検証。倍率OCRで動くはずだが
    実戦で「がくっと下がった」等の後に一度開いて確認したい
- タイプ混同の追加修正: あく⇔ゴースト等、実フレームからテンプレート追加で
  選出画面5枚60/60。弱い一致 (劣化フレーム) は確定せず次フレームで再試行する方式に変更

### 優先度中 (精度向上)

- [ ] タイプアイコンのテンプレート未収録2種 (どく/いわ) は色のみで判定中。
      出現したら `python -m tools.make_type_templates <選出画面フレーム> "<正解ラベル>"`
      でテンプレート追加
- [ ] HUDの未抽出要素: 残数ボール(相手の残り匹数)、持ち時間タイマー、
      名前横の状態異常アイコン。ゾーンは `vision/zones.py` に定義済み(balls/timer)だが
      検出ロジック未実装
- [ ] 選出画面の持ち物OCRが汚い文字列を残す場合がある(「フイチ;ウナイトソ」等。
      item_id=megastone へのフォールバックは機能している)。ナイト系語尾の
      正規化強化か、判明済み持ち物リストとの再照合で改善可能
- [ ] まもる等のPP最大値が読めないケース (「8/8」の小さい分母)。現状は現在値のみ

### 優先度低

- [ ] `vision/state.py` の `reset_on_switch_out` の keep 変数が未使用 (やどりぎの扱いを
      簡略化した名残)。交代時に残すべき状態(とんぼ返り等)の整理
- [ ] pokedb opendata は現行シーズン(s4)が未公開のためs3を使用中。シーズン公開後は
      自動で最新に切り替わる(最新番号から遡って探索する実装)が、初回は要確認
- [ ] launchd(毎日06:30の使用率DB自動更新)は plist 同梱済み・未ロード。
      有効化はREADME「データの定期更新」の2コマンド

---

## 2. ランクマッチのルール変更(レギュレーション/シーズン更新)時の対応

チャンピオンズはシーズン制(M-1, M-2, ...)+レギュレーション制(M-A, M-B, ...)。
レギュ変更では使用可能ロースター・新ポケモン・新メガシンカ・技/特性のリバランスが入る。

### 対応チェックリスト (上から順に)

1. **使用率DBの更新** — `bash champions_agent/scripts/update_usage_db.sh`
   - championsbattledata は `defaultSeason` を返すため新シーズンへ**自動追従**
   - pokedb opendata もシーズン番号を最新から遡って探索するため自動追従
   - 新レギュの対象ポケモン一覧はAPIのポケモンリスト(現235匹)に反映されるので、
     取得件数がログで大きく変わっていないか確認
2. **静的データの再生成** — 新ポケモン/新フォームが追加された場合:
   ```bash
   python -m vision.data.fetch_jp_names   # 日本語名辞書
   python -m advisor.data.fetch_dex       # 種族値/タイプ/技
   ```
   - PokeAPI未収録のゲーム独自要素(例: 新規メガの種族値)は反映されない。
     その場合は `advisor/data/dex.json` に手動でエントリ追加
     (`championsbattledata.com` の metadata CSV にメガ含む種族値があるため転記可能)
   - 新規メガストーン(「〜ナイト」)は辞書未収録でも `item_id="megastone"` に
     フォールバックする実装のため、メガ可能判定は壊れない
3. **技・特性のリバランス** — ゲーム内で威力/仕様が変わった技は
   `advisor/data/dex.json` の該当エントリを手動更新(セクション3の補正テーブル化が本命)
4. **UI変更の確認** — 画面レイアウトが変わった場合:
   `python -m tools.run_images images/<新スクショ>` でシーン分類と抽出を確認し、
   ズレは `tools.debug_zones` → `vision/zones.py` で修正
5. **ルール定義の更新** — `champions_agent/config.py` の `Regulation`
   (選出数/テラス可否等。現状は暫定値のまま)
6. **回帰テスト** — `python -m tests.test_events && python -m tests.test_advisor`

### シーズン切り替わりだけ(レギュ据え置き)の場合

1のDB更新のみでよい。日次launchdを有効化していれば無操作で追従する。

---

## 3. 機能拡張の方針

### 3-1. 抽出系 (vision/)

- **「場の状況」画面(Xボタン)対応** — 能力ランクの青/赤▲と倍率、場の効果、天候残り
  ターンが確定値で表示される画面。メッセージ推定に頼っているランク補正を
  確定値で上書きできるため、抽出精度への効果が最も大きい拡張。
  新シーン分類 + ゾーン定義を追加する(スクショ採取が先決)
- **「バトル詳細情報表示」オプション活用** — ゲーム内設定でONにすると画面左上に
  天候等の残りターンが常時表示される。ここを読めばターン管理が正確になる
- 相手の性別・レベル表示、こだわり系のロック技追跡(同じ技しか出ていない検出)
- 対戦結果画面の検出 → 勝敗ログの自動記録(学習・振り返り用DB)

### 3-2. アドバイス系 (advisor/)

- **能力ポイント分布の反映** — 使用率DBの spread(0〜32スケール、32≒努力値252)を
  ダメージ計算のEV仮定に使う。現状は攻撃系252固定仮定なので、
  耐久振りが多いポケモン(カバルドン等)への与ダメを過大評価しがち
- **チャンピオンズ・リバランス補正テーブル** — `advisor/data/champions_patch.json` の
  ような差分ファイル(技威力/命中/仕様変更、まひ12.5%等)を dex.json に
  オーバーレイする仕組み。攻略サイトの変更点一覧から手動メンテ
- **2手先読み(簡易ゲーム木)** — 現在は1ターン期待値。相手の交代・こちらの
  交代後の展開まで読む expectimax 2-ply 化。foul-play 同様、
  ダメージロールのグルーピングで計算量を抑える
- **選出アドバイス** — 選出画面で相手6匹のタイプ(+判明した種族)と
  teammate_usage/meta_sets から、3匹の選出候補と先発をスコアリング。
  現在DBに材料は揃っており、エンジンだけ未実装
- **メガシンカ判断の明示化** — 「このターンにメガすべきか」をメガ後種族値での
  ダメージ計算比較で提示(mega_used と メガ形態dexは既に整備済み)
- PP管理(相手の技PP推定、まもる連打の成功率低下)、状態異常の行動確率
  (まひ12.5%/こんらん等)を期待値に織り込む

### 3-3. 学習系 (champions_agent/)

#### セルフプレイRLの障壁 (2026-07-19 スモークテストで実測)

**解消済み** (この日付で修正、`gen9customgame` でPPO学習が実際に回ることを確認。
実測スループット約244 step/s ≒ 4バトル/秒 → 夜間8時間で10万バトル規模が可能):

1. team_builder が `source="smogon"` 固定で新使用率DB (championsbattledata) を引けず即死 → source指定を撤廃
2. 能力ポイント (0-32) をEV換算せずShowdownへ渡していた → ×8換算+252クランプ
3. チャンピオンズ専用メガ種 (Dragonite-Mega等)・専用メガストーン (Dragoninite等) が
   gen9に存在せずチーム全拒否 → ベース種・実在アイテムへのサニタイズを追加
4. gen9ouの検証 (EV合計510制限・世代間技継承・0EV警告) に引っかかる → `gen9customgame` を使用

**2026-07-19 追加解消分** (常時併用+観測改善の実装。スモークテスト済み: 365step/s):

- (旧7) **3v3・Lv50化** — チームを3体・`Level: 50` で生成する方式で再現
  (`TRAINING_TEAM_SIZE`)。poke-env 0.10 が6体前提で交代インデックスがIndexErrorに
  なる問題は `ChampionsSinglesEnv.action_to_order` のフォールバックで解消
- (旧8) meta_sets の nature/evs 分離 → 「値のある行の最上位」を独立取得して合成。
  build_meta の再実行時重複も解消 (DELETE→INSERT)
- (旧9) **観測ベクトルを227次元に全面刷新** (`encoders.encode_battle`):
  自分の技4スロット (威力/命中/優先度/分類/STAB/相性倍率/PP) + 両者のタイプ/種族値/
  ランク/状態異常 + 控え + 相手の判明技 + 設置技/壁/おいかぜ + 天候/フィールド/TR +
  素早さ比較。battleオブジェクト直読みでDBアクセスなし
- (旧11) **ポート分離で常時併用可能に** — Showdownは8100 (`SHOWDOWN_PORT`)、
  アドバイザーは8000のまま。同時起動でのPPO学習を実測確認
- (旧12/13) **夜間バッチ整備** — `scripts/train_nightly.sh` (caffeinate/Showdown自動起動停止/
  `--resume`継続学習/観測空間非互換チェックポイントの自動退避/評価/ログ)

**2026-07-19 追加解消分 その2 (メガ/リバランス再現 + selfplay移行)**:

- (旧5/6) **メガシンカ・リバランスの再現を達成**。アップストリームShowdownに
  公式のchampions mod (`data/mods/champions/`) が存在することを発見し、
  学習フォーマットを **`gen9championsbssregmb`** ([Gen 9 Champions] BSS Reg M-B) へ変更。
  - メガシンカ (交代後も継続するチャンピオンズ仕様)・新メガストーン (dragoninite等)・
    まひ1/8・ねむり2-3T・こおり1/4+確定解除・championsの技プール/リバランスすべて再現
  - Flat Rules = 6体構築→3体選出・Lv50・種族/アイテムクロース
  - チーム生成をchampionsネイティブに適合 (**EV欄は能力ポイント0-32/合計66として
    そのまま渡す**・アイテム重複解消・floette→Floette-Eternal等)。検証器30/30合格
  - poke-envが新フォームを知らず落ちる問題は `champions_dex_patch` で解消
    (Showdownから種族1517/技938をエクスポートして実行時注入。
    再生成: `node tools/export_champions_dex.js > champions_agent/data/champions_dex.json`)
- (旧15) **population-based selfplayへの自動移行を実装** (`train/opponent_pool.py`):
  - vs Random勝率が**ゲート0.75**を超えたチェックポイントをプールへスナップショット
    (性格ごと最大5世代)
  - プールに1件でもあれば、学習の対戦相手が自動でRandom→「過去チェックポイントの方策
    (バトルごとに新しい世代を優先して抽選、ε=25%でRandom混合)」へ切り替わる
  - チームは両陣営とも**バトルごとに使用率メタから再生成** (ChampionsTeambuilder)
  - 夜間バッチに組み込み済み: 学習→評価→ゲート判定→プール更新が毎晩自動で回る
  - 検証ツール: `python -m tools.smoke_selfplay` / `python -m tools.validate_teams`

**2026-07-19 追加分 その3 (選出アドバイス+種族推測+RL品質検査)**:

- **選出画面アドバイザー実装**: 選出進捗「N/3」判定 + C(6,3)総当たりの選出提案。
  相手はタイプアイコンから**使用率ベースで種族を確率推測** (例: ほのお/ゴースト→
  ラウドボーン54%/ソウブレイズ42%) し、候補種族の実種族値・予測技構成への
  ダメージ計算で評価。**メガストーン持ち2体以上の選出にはペナルティ**
  (メガは1試合1回)。バトル中アドバイスも種族未判明時は最有力候補で評価
- **RL方策の品質検査 (tools/probe_policy)** で重大な発見: 高勝率 (93%) は
  「無効アクション→サーバーデフォルト=技1連打」の産物で、方策自身の
  技選択はランダム水準だった。推論側は合法手マスキング (行動分布から合法手のみ
  argmax) を実装済み

**2026-07-19 追加分 その4: MaskablePPO化 (旧9b、解消済み)**:

- sb3-contrib の **MaskablePPO** に移行。`MaskedSingleAgentWrapper.action_masks()` が
  毎ステップ合法手マスク (交代/技/メガのみ有効、Z/ダイマ/テラス枠は常に無効) を供給
- 旧PPOチェックポイントは非互換のため自動退避→新規学習
- 効果 (4kステップ時点の probe_policy): 攻撃技選択率 0.02→**0.98**、
  抜群技選択率 0.01→**0.38** (ランダム基準0.23を超え、初めて意味のある方策に)

**残る障壁**:
10. 報酬の重みチューニング — 学習データが溜まってから着手予定
14. train_selection.py (選出方策) は未スモークテスト
16. レギュレーション更新時: Showdown側のchampions modはupstream追従が必要
    (`cd pokemon-showdown && git pull` + `node tools/export_champions_dex.js` 再実行)。
    M-A形式は `championsregma` mod として併存


- **RLの位置づけはアドバイザーの補完** (探索ベース > RL が2025-26の実証結果)。
  有望な使い道:
  1. アドバイザーの評価関数(スコア重み)をセルフプレイ勝率で自動チューニング
  2. 選出方策(policy_selection)の学習 — 探索が効きにくい不完全情報の意思決定
- Showdown側にチャンピオンズ準拠mod(まひ12.5%、PP固定、メガ持続)を作ると
  学習と実環境の乖離が減る(`pokemon-showdown/data/mods/` に差分実装)
- meta_sets ベースのチーム生成は使用率DB差し替えで実環境化済み。
  selfplay の本格運用(population-based)は未着手

### 3-4. UI/運用

- アドバイス履歴と「実際に選んだ手」の記録 → 振り返り画面
- アドバイスの信頼度表示(相手の型が判明済みか予測かで色分け。
  threats の revealed フラグは配信済みでフロント未表示)
- 音声読み上げ(推奨手だけ喋る)— プレイ中に画面を見なくて済む
- スマホ単体運用(Mac不要化): サーバーをクラウド化し、スマホの画面収録
  ブロードキャストを直接受ける構成の検討

---

## クレジット (データ利用時の表記義務)

- Battle data provided by [Pokémon Champions Battle Data](https://championsbattledata.com)
- 上位構築データ: [バトルデータベース チャンピオンズ](https://champs.pokedb.tokyo)
  公式オープンデータ(詳細ページのスクレイピングは同サイト規約で禁止)
