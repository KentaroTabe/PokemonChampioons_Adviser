# 接続テスト チェックリスト (2026-07-24 更新分)

## 2026-08-22 持ち越し課題の一括解消 (テスト外)

しばらく接続テストを行わない期間に、第5〜8回の残課題を精査して解消した。

| 持ち越し | 判断/対処 |
|---|---|
| ロトム系フォームの同名混同 (第5回) | **修正**: イベント/HUDの帰属を「同族候補 (一致名を内包するスロット) を含めて場の個体を優先」に。HUD名が基本形表記でも、場のフォーム個体 (rotomwash等) を基本形スロットへ切り替えない |
| decision_audit の死に出し帰属検証 (第5回) | **検証済み・監査は正当だった**: 015832 turn5 はいかく被弾後に助言が switch:キラフロル へ正当に更新されており (押下1.5秒前の確定表示)、ユーザーのブレイブバードは実際に逸脱。第5回記録の「生ログでは従っていたのに不一致」という記載が誤り (私の検証ミス) と判明したため訂正する。監査ロジックの変更は不要 |
| 選出モデルの学習分布 M1/M2 (第5回〜) | **M1解消・M2是正中**: 収集2×2500戦+再学習を実行し、未知チーム検証 **+6.94%** (54,000件・272チーム分割) が META に記録され M1 ゲート通過。ただし初回収集は「現在の6体」の推定が旧構成 (キラフロル入り) に引きずられて走った — 推定を直近対戦ログの選出ロスター最優先に修正し (evaluate_team)、マスカーニャの最小エントリ追加で自分側の名前解決も回復させたうえで、正しい構成での再収集を実行 |
| つよさ表示→watch 誤分類 (第6回) | **方針転換で解消**: watch/つよさ/パーティ一覧は同一オーバーレイ群で色特徴が共通なため、分類器での分離は断念 (誤爆リスクが利益を上回る)。害の実経路だった my_team 自動更新を「同一内容が2フレーム連続で読めたときのみ書き込み」に変更して遮断。その他の書き込みは既存の帰属ガード (_watch_target・側柱のスプライト照合) が守る |
| ホーム画面/黒画面/選出→field 誤分類 (第6-8回) | **整理**: field は既定クラスで、誤分類時の実害経路は message OCR のみ (ランク画面検出は正しく動作、その他の文言はパーサが棄却)。リザルト画面での hp 誤イベントは下記ゲートで遮断 |
| battle_end_rank 後の hp 誤イベント (第8回) | **修正**: field シーンの軽量HP読みを battle_active 中のみに制限 (次戦の battle_hud 抽出が battle_active を立て直すので自己復帰) |
| メガ表記の取り逃し (第6-7回: メガハッサム/カイリュー[メガ]) | **修正**: HUD名がメガ形 (メガハッサム等) に解決されたら is_mega を立てる (メガシンカ文言の取り逃しを名前で補完。枠は基本形のまま維持) |
| サザンドラへのタイプ混入 (第8回) | **調査済み・非再現**: 該当フレーム3枚 (fc_1787309964 / frame_1787310116 / 1787310138) を現行ガードで再現実行し汚染は起きない。発生時間帯の中間フレームが残っておらず経路の確定は不能。防御として my_team 2フレーム確認・field ゲートを追加し、再発観察に切替 |
| 「たおれた」取り逃し / active取り違え (第8回) | **負荷起因と整理**: 提案ジョブの別プロセス化 (第8回修正) で処理率が回復する見込みのため、コード変更なしで再発観察 |
| ミミッキュHPの単発齟齬 (第7回) / fc画面の斜体数字桁化け (第5回) | 見送り継続 (単発観察 / OCR救済不能。主経路の毎フレームHUD読みが機能しているため) |

回帰: events / hud_attribution / hp_settle / opp_identity / advisor /
session_split / decision_audit 全緑 (新規テスト5件)。

## 2026-08-21 第8回追従テスト (夜) の結果と修正

2戦+構築提案の採用を挟む実運用初回 (1戦→段階1提案→パーティ変更→続戦)。
決定9 / 助言あり78% / 一致57% / 時間内100%。指摘4件+監査から同日修正6件。

| 指摘 | 根本原因 | 対処 |
|---|---|---|
| とんぼがえり後の推奨選出がなされない | 交代先選択の場面を判別する仕組みが無く、技トップの通常助言のまま | **修正**: 対面操作技 (とんぼ/ボルチェン等8種) の使用イベントで pending_pivot_switch を立て、engineは交代限定の助言に切替。交代完了・次ターン到達・対戦終了で解除 |
| リザルト確認の数秒後に提案を実行したい | check_battle_active が outcome 記録自体を「対戦中シグナル」に数え、窓いっぱい (60秒) ブロック | **修正**: outcome を終端シグナル化 (最新記録が outcome なら終了扱い)。第7回のランクキー→outcome即時記録と合わせ、リザルト後数秒で実行可能に |
| 提案パーティに持ち物なしのポケモン (マスカーニャ) | 種族入替時のアイテムクローズ衝突で None にしていた | **修正**: その種族の使用率次点で未使用の持ち物へ差し替え (_alt_item) |
| 構築変更後に選出・対戦画面を認識しない | 複合: (a) 提案ジョブをサーバープロセス内 (GIL共有) で実行し、フレーム処理率が半減 (取りこぼし54%→76%、当該対戦の助言あり33%) (b) 新規マスカーニャが my_team **未登録のまま**で、自分側の画面名解決 (登録種族に限定) の候補から外れ、activeを特定できなかった ※8/22の精査で訂正: 初回記載の「空エントリで has_build ガードが誤作動」は誤り (エントリ自体が存在しなかった。確認コードが不在と空を区別していなかった) | **修正**: (a) 提案ジョブを別プロセス化 (nohup相当。進捗はログtail配信、サーバー再起動でもジョブ継続) (b) マスカーニャの最小エントリ (種族のみ) を追加して名前解決を回復。has_build の実質登録要求と _expected_my_max のEV必須化は、部分登録シェル (watch自動更新が作る特性/持ち物のみのエントリ) への予防として維持 |

非欠陥の確認: 提案のライチュウ「ノーガード」は登録どおり (チャンピオンズ
公認特性。今夜の再登録で特性が更新されていた)。むしろ engine 側が命中50の
まま減点していたため、**ノーガード必中を実装** (攻守どちらが持てば命中100%)。

### 採用フローの注意 (今回の実測から)

- **マスカーニャの登録が空のまま** (特性/持ち物/技=None)。パーティ編集か
  もっと見る画面で登録を完了すること (空だと認識品質・助言品質が下がる)
- 受入検定「差は誤差の範囲」(+0.025±0.063) での採用はユーザー判断で可
  (今回採用)。有意差を求めるなら --accept-battles を増やして再実行

### 残る監査指摘 (次回課題)

- [高] サザンドラのタイプに いわ/どく (キラフロルのタイプ) が混入した
  時間帯 (20:01:52以降。fc_1787309964 / frame_1787310116 で要再現調査。
  type_changed 抑止の副作用の可能性も含めて切り分ける)
- [高] battle_end_rank 済みのリザルト画面で hp 変動イベントが発火
  (画面の数値をHPとして誤読。battle_active=False 中の抽出ゲートを検討)
- [高] 「サザンドラはたおれた!」明示後も100%保持 / active取り違え —
  処理率低下時のメッセージ取り逃しファミリー (別プロセス化で頻度低下を
  見込む。次回テストで再発観察)
- シーン誤分類ファミリー継続 (全面黒画面→field 等)

## 2026-08-21 第7回追従テスト (夕) の結果と修正

2戦。決定12 / 助言あり100% / 一致92% / 時間内100% / **機械検出の矛盾候補0件**
(第6回の相手0%往復・偽ひんし修正が実地で有効)。手動確定プルダウンは
「快適」(ユーザー所感 — 第6回修正の実地確認)。指摘2件+提案1件を同日実装。

| 指摘/提案 | 根本原因 | 対処 |
|---|---|---|
| ミミッキュの持ち物がビー玉 (ユーザー指摘) | ポップアップ「{名前}の{持ち物}」の分割が最後の「の」から早い者勝ちで採用され、「いのちのたま」自身の「の」で切れた「たま」が ビーだま(marble) に誤解決 (実ログ: item_player_marble ← 「ミミッキュのいのちのたま」) | **修正**: 全ての「の」分割を試し最高スコアの解決を採用 (完全一致のいのちのたまが必ず勝つ) |
| はたきおとされた持ち物を保持したまま (ユーザー指摘) | はたきおとす被弾の処理が存在せず持ち物が残存。仮に消しても登録バックフィル・使用率予測・型推定が復活させる構造 | **修正**: knockoff イベントで持ち物を喪失させ item_removed を立てる。バックフィル/使用率予測/型推定の3経路すべてで復活を抑止 (交代しても回復しない) |
| 対戦終了後のランク画面を終了キーに (ユーザー提案) | 勝敗文言はスキャン頻度・シーン分類に依存して取りこぼす (今回も「勝負に勝った!」表示中の未発火を実測。当該対戦は outcome不明になった) | **実装**: ランク/レート表示の検出で battle_end_rank を発火し battle_active を解除。ロガーはその場で勝敗を確定 — 終局時点のひんし数と同フレームで捕捉したレート増減を推定に使える (次戦の選出まで待たない) |

### 残る監査指摘 (次回課題)

- **シーン誤分類ファミリー継続**: ホーム画面→field / 選出画面→field (再発) /
  つよさ表示→watch (継続)。今回の新パターン: 入場演出のカメラパン中に
  選出ロスター抽出が走り、相手先発をギャラドスと誤同定 (後にペリッパーの
  実HUDで自己修正)。分類器の分離が最優先の残課題
- メガハッサムに[メガ]表記が付かない時間帯 (中確度) — メガ表記
  ファミリーの持ち越し (ラグラージには付与できていた)
- 自分ミミッキュ 130/162=80% を100%と主張した時刻あり (単発。毎フレーム
  読み導入後の残存ケースとして再発観察)

## 2026-08-21 第6回追従テスト (昼) の結果と修正

3戦 (勝1/負1/不明1)。決定29 / 助言あり100% / 一致79% / 時間内100%。
ユーザー指摘3件 + opus視覚監査 (30フレーム・乖離11件) から同日修正6件。

| 指摘/検出 | 根本原因 | 対処 |
|---|---|---|
| 変幻自在で変わったタイプがすぐ元に戻る (ユーザー指摘) | backfill_player_static が毎heavyフレームで図鑑タイプへ強制訂正 (アイコン誤分類の訂正機構が観測タイプ変化を区別しない) | **修正**: type_change イベントで type_changed フラグを立て訂正を抑止。タイプ変化は交代で戻るため交代アウトで解除 |
| 構築提案が常に「対戦中」扱いで使えない (ユーザー指摘) | 「フレーム受信中+シーンが対戦系」判定は、メニューが field 等に誤分類されるためキャプチャ中は常時真 | **修正**: 対戦ログの記録内容で判定する check_battle_active に差し替え、窓を60秒に短縮 (対戦終了から約1分で実行可能) |
| 挑発をうまく使えていない (ユーザー指摘) | 素点15固定。受けのブラッキー相手でも攻撃技に埋もれ、RLが78-84%で挑発を推してもブレンドで届かず (ユーザーは助言に逆らって挑発し、それが正解) | **修正**: 相手の技プール (判明+使用率予測) の変化技重み比率に比例した加点 (上限35)。攻撃技主体の相手は従来どおり低評価 |
| ガブリアス21%に0%が書かれ往復・偽ひんし化 (opus[高] 矛盾候補) | (a) _set_hp の「初回は即反映」が0%にも適用され、登場直後 (HP未知) に演出中の空バーが即コミット (b) fieldシーンの軽量HP読みに名前照合ガードが無く、長い演出中に安定3回条件を満たして0%を再コミット | **修正**: 初回0-3%読みは即コミットしない / fieldパスにHUDと同じ帰属ガード (名前不一致・名前未読+大変化は書かない) / watch側柱の相手0%も裏付け必須化+同一個体の重複行スキップ |
| 自陣ステロ未取得 → 自分HPの被ダメ予測が1/8ずれ (opus[高/中]) | 場の状況の「ステルスロック状要」(態のOCR化け) が『状態/状感を含む行』ゲートで棄却 | **修正**: ゲートを「状」1文字に緩和 |
| 場の状況画面の自分HP 106/162 を読めず100%主張 (opus[高]) | 斜体数字の桁化け "T0G/152" (バー照合でも救済不能) | 見送り: fc画面のHPは補助経路で、主経路 (HUD毎フレーム読み・第5回導入) が機能しているため優先度低 |

### 残る監査指摘 (次回課題)

- **「つよさの表示」パーティ画面が watch に誤分類** (frame_1787281683 /
  1746 / 2258 / 2925 の4フレームで安定再現。時刻ズレでは説明不能)。
  誤分類中は watch 抽出が走り誤読の温床になるうえ、watch heavy は
  my_team の自動更新を持つため誤書き込みリスクがある (今回は実害なしを
  実測確認済み)。COMMANDカウンタ表示等での分類分離が必要
- ホームメニュー→field (frame_1787281531)、選出画面→field
  (sel_1787282161) の誤分類 各1件
- ひんしのムクホークが active に残存した時間帯 — 上記誤分類フレームの
  状態記録アーティファクトで同根とみられる
- カイリュー[メガ]表記の判定不能 (低確度・フレーム不足)。前回からの
  持ち越し (メガ表記の取り逃しファミリー)

## 2026-08-20 第5回追従テスト (深夜) の結果と修正

9戦実施。決定52 / 助言あり100% / 時間内100% / 一致65%。不一致の大半は
ユーザー自身の判断による逸脱 (「途中で推奨が当てにならない」との報告)。
ログ+opus視覚監査 (30フレーム、乖離11件) で追跡し、確定した欠陥6件を
同夜に修正した (回帰テスト付き)。

| 欠陥 (実測) | 根本原因 | 対処 |
|---|---|---|
| 残24%の相手に等倍53%を抜群106%より上位に推奨 (エナジーボール>だいちのちから) | KO圏では実効ダメージが残HPで頭打ちになり、同じKO圏の技の順位がRL%の揺らぎだけで決まる | **修正**: KO圏の技に突破余裕 (余剰ダメージ×0.15、上限20点) を加点。HP誤読に対して安全側 (engine.py KO_MARGIN_*) |
| 自分のHPが最大63秒古いまま助言計算 (実62%のライチュウを100%扱い。opus[中]×2) | 自分HP実数の読取が heavy フレーム限定で、2回安定の確定に時間がかかる | **修正**: extract_my_hud を分離し、command/move_select/battle_hud の毎フレームで読む |
| ムクホークのHPが100%固着 (登録から計算した最大161 vs 実測181) | 能力ポイントの登録が古く、理論最大HPとの照合が全読取を棄却し続けた | **修正**: バー割合と一致する同じ実測が3回続けば実測を採用し「再登録を確認」の警告ログ (MY_MAX_ADOPT_READS)。登録の更新はユーザー作業 |
| 満タンのロトムに0%が書かれ偽ひんし (opus[高] 矛盾候補1) | ゼロ読みガードが「既知HP>5%」の個体のみ対象で、HP未知の新規スロットは素通り | **修正**: HP未知の個体も裏付け (2連続ゼロ読み or faint) 必須に |
| 不参加のムクホークが党リストに生成され161/161混入 / サザンドラ⇔キラフロルのHP入れ替わり (opus[高]) | 場の状況抽出が、種族行を解決できない画面で active へフォールバック+新規メンバー作成して書き込む | **修正**: 既存パーティメンバーへ解決できた場合のみ書き込み (フォールバック/新規作成を廃止) |
| 勝敗が9戦中6戦 unknown | リザルト画面を飛ばすと勝敗文言もレート変化も取れない | **修正**: ローテーション時に前戦最終盤面のひんし数 (3落ち) から推定し inferred 付きで記録 |

非欠陥の確認: スコアの跳ね (同一手64.6→116.8等) は相手HP更新による
KO圏判定の正当な変化。第4回修正のミラー交代側判定・トレーナー名形式は
実戦で正常動作 (フォーム接頭辞つき「ホウエンチャンピオンライボルト」も
switch_opponent 発火)。推奨は全件登録技の範囲内だった。

### 残る監査指摘 (次回課題)

- **ロトム系フォームの同名混同**: 相手ロスターにロトムとウォッシュロトムが
  並ぶと、文言/HUD名「ロトム」の帰属が先頭一致へ寄り、ひんし/HPが別
  フォームに付く (opus: 0%の枠が1番目↔3番目で食い違い。ドラミドロ53%も
  同族の疑い)。タイプアイコン/スプライトでのフォーム分別が必要
- decision_audit の死に出し前後の帰属検証: 015832 turn5 は生ログでは
  推奨どおり (死に出しtop=ムクホークへ交代→top=ブレイブバード使用) なのに
  「不一致 (推奨switch:キラフロル)」と出た。決定窓と表示中助言の対応付けを
  確認する (一致65%は実態より低い可能性)
- 選出 model_pick が trained:false: ペロリーム入りの現チームが選出モデルの
  学習分布外。collect_selection→train_selection の再学習を検討
- ローテーション境界の監査アーティファクト: 前戦の状態値が次戦の選出
  フレームと照合される (sel_1787160057)。実害なし、監査側の窓の問題

## 2026-08-19 残課題の修正 (第5回テスト前・テスト外)

第4回の「残る監査指摘」を実装で解消した (a013ea4a)。接続テストは未実施。

| 残課題 | 対処 |
|---|---|
| 「助言なし」の交代決定 (turn0/2/5 の switch:?) | **原因特定: 独立した欠陥ではなかった**。実体は相手の繰り出し (「RX78ー2はサザンドラを繰り出した」) が switch_player に誤帰属され、実在しない自分交代として監査に載っていたもの。下の側判定修正で解消 |
| 相手の「繰り出した」で自分側activeが書き換わる | **修正**: 交代の側判定をメッセージ形式最優先に (「ゆけっ/こちらは」=自分、「相手」「(トレーナー名)は〜繰り出した」=相手)。種族の所属照合はミラーマッチ (相手も同種族を所持) で破綻するため最後のフォールバックに格下げ |
| 自分HPが相手スロットへ混入 (自ミミッキュ33%→相手ロトム34%) | 該当時間帯のログにロトムのHP記録そのものが無く単独の再現は不能だが、同じ側誤帰属ファミリーで上記修正の範囲とみられる。次回テストで再発監視 |
| 相手ひんし誤検出の継続 (生存ドリュウズ100% / タスキ1%サザンドラ) | **修正**: 生存中の相手の突然の0-1%読みは faint イベントか連続2回の読みが揃うまで書かない。ひんし化は0.5%以下のみ (タスキ1%生存はひんしにしない)。監査側の検出器も status=fainted のみを「ひんし」とみなすよう修正 (hp<=1 判定がタスキ生存を「蘇生」矛盾として量産していた) |
| ばけのかわの定数ダメージ取り逃し | **修正**: ばけのかわ popup 検出時に最大HP1/8 を即時反映 (以後の実読みで上書きされる) |
| メガカメックスの[メガ]表記取り逃し / 相手アイコン誤同定 | **持ち越し**: メガシンカ文言がフレームに全く写っておらず (メッセージ欠落ファミリー)、文言以外の裏付け (HUD名の[メガ]接頭辞等) を試すにも再現データが不足。次回テストでフレームを確保してから対処 |

回帰: test_events / test_hud_attribution / test_audit_anomalies (新規) /
test_opp_identity / test_decision_audit 全緑。

## 2026-08-19 第4回追従テスト (深夜) の結果と修正

6戦実施 (勝ち1を含む)。決定51 / 一致91% / 時間内100% / 助言あり90%。
安定化ゲート稼働後の初テストで、churnは各戦1〜3件 (情報由来の正当な
反転を含む) に低下。

| 指摘 | 対処 |
|---|---|
| 自分の技は登録があればOCRに頼るべきでない (OCR解決の誤りが実在しない技を提示) | **修正**: 登録技を正とし、技選択画面のOCRは制限付き再解決でPP/相性ヒントの対応付けにのみ使用。登録外へ解決された行は誤読として捨てる。画面と登録が3行以上不一致なら「もっと見るで再登録」を促すログ (型変更に黙って追従しない)。今夜のログに状態混入は無かったが、8/11ログに実例 (ムクホークへ bulkup/closecombat 等) |

### 残る監査指摘 (次回課題、前回からの持ち越し含む) → 8/19 に対処済み (冒頭の表)

- 「助言なし」の交代決定 (turn0/2/5 の switch:?) — 死に出し/交代フローの
  助言配信カバレッジ。安定化ゲートのprovisional除外との関係も確認する
- **相手の「繰り出した」で自分側activeが書き換わる** (第4回opus:
  相手トレーナー名「RX78−2」の化けで側判定を誤り、自分activeが
  相手のサザンドラ0%になった)。修正案: 繰り出し種族が自分の登録チームに
  無く相手ロスターに有るなら側を強制する
- **自分HPが相手スロットへ混入** (第4回opus: 自ミミッキュ33%が相手ロトム
  34%として記録され、ミミッキュ自身は100%固着) — 上と同族の可能性
- **相手ひんし誤検出の継続** (第4回opus: 生存ドリュウズ100%にひんし判定 /
  きあいのタスキで1%残ったサザンドラを0%ひんし扱い — 自らタスキを
  検出した直後に矛盾)。相手側のHP0→ひんし確定にも裏付け強化が必要
- ばけのかわの定数ダメージ (最大HP1/8) が取り逃されHP過大 (第4回opus)
- メガカメックスの[メガ]表記取り逃し / 相手アイコン誤同定 — 持ち越し

## 2026-08-18 第3回追従テスト (23時) の結果と修正

3戦実施。**一致率100% (20決定中20)**・時間内100% — 第2回までの修正群は
実地で完全に機能。ユーザー指摘3点を同夜に修正した。

| 指摘 | 根本原因 (ログで特定) | 対処 |
|---|---|---|
| 対戦中に推奨選出が時々出る | 対戦中の standby/selection 誤分類フレームでサーバーが選出助言を発火 (turn進行中に5回実測)。battle_active ゲートが無かった | **修正**: `should_advise_selection` に battle_active 条件を追加 |
| 技画面を開くまで交代推奨→開くと技推奨 | stateの技が未読取の間、**RLヒントの合法手が交代のみに縮退**し確率が交代に集中 (実測: command画面で交代RL100% → move_select で剣舞RL70%へ移動して反転)。登録技フォールバックがengineのみでRL未配線だった | **修正**: RLにも登録技フォールバックを注入 (浅いコピー) |
| アドバイスがコロコロ変わる → 安定するまで出力待ちにして欲しい | 状態更新ごとの再計算で最善が細かく揺れる (churn 各戦3-4件)。ヒステリシスだけでは情報由来の揺れを吸収しきれない | **修正**: 安定化ゲート — 同じ最善が連続2回出るまで「🔄 推奨を確定しています…」を表示。サーバーはprovisional時に即再計算 (追加遅延≈1フレーム)。3秒超の揺れは注記つきで表示 |

### opus視覚監査の新規検出 (次回の修正候補)

- 相手HPの系統誤読: ミミロップ 100%を78%と3分間継続 / フシギバナ 71%を87%
- **相手ひんしの誤検出**: 生存中のウルガモス (HUD 48-51%) にひんし記録
  → 「ひんし後の再表示」矛盾を量産。ガブリアス38%再表示は逆に38%側が誤り
- 自分キラフロルのひんし (0/159表示) を56%と主張した時間帯あり
- リザルト画面〜VS画面の約4分を対戦ログに含めた (battle開始判定)
- シーン遷移の時刻許容差による low 指摘多数 (非欠陥扱いでよい)

## 2026-08-18 第2回追従テスト (22時) の結果と修正

2戦で終了。2戦目は決定監査パーフェクト (一致100%・欠陥0)、
1戦目とopus視覚監査で新たな欠陥系を検出し、同日中に修正した。

| 報告/検出 | 根本原因 (ログ・フレームで特定) | 対処 |
|---|---|---|
| 登録済みの技を把握しない / 技未把握のまま選出助言 | `get_my_build` が技を正規化で落とし、どこからも未参照 (技の供給源が画面OCRのみ) | **修正**: engineの技評価と選出評価の自分側に登録技フォールバック/優先を配線 |
| 努力値を把握しない | 理論最大HPが全員一致 (169/161/162✓) — EV/性格は適用されていた。体感のズレは下記ひんし取り逃しでHP表示が古かったのが主因とみられる | 非欠陥 (機構上適用済み)。次回、被ダメ%の実測照合で確認 |
| こだわり系で別の技を推奨 | 技ロック未実装 (倍率のみ対応) | **修正**: last_move機構でロック+注記 |
| (継続) 死に出しで技推奨 / サザンドラのひんし未認識 | 2戦目22:20:40頃、ひんしメッセージと次の「ゆけっ」の両方を取り逃し。HUD名解決で事後的にactiveは復帰したが、サザンドラは100%のままベンチ汚染。1.5秒のheavy間隔では交代メニュー滞在中にゼロ読み確定 (3回) が間に合わない構造 | **修正 (多層)**: 側柱毎フレーム読み+ゼロ読み2回化+hp_uncertainマーク+advisor減点警告 |
| 幽霊スロット (実在しないオーロンゲ9.8%がslot0に出現しactive乗っ取り) | 1回の化けHUD読みで switch_to_species が新規枠を作成 (ロスターロックは満枠時のみ) | **修正**: 初登場種はHUD名の連続2回一致で受け入れ |
| ゲッコウガのタイプ変化未反映 | 「〜タイプになった」イベント未対応 | **修正**: type_change イベント追加 (変幻自在/リベロ) |
| 入場演出で switch_opponent 誤発火 (opus指摘) | 実ログの発火文言は正当な初手繰り出し (「暑はガプリアスを繰り出した」turn0)。監査の時刻許容差による誤指摘 | 非欠陥 |
| コマンド↔技選択の誤判定 (opus指摘) | 当該フレームを現分類器にかけると全て正解 (command/pills=0、move_select/pills=4)。シーン遷移と保存時刻のズレ | 非欠陥 (許容差) |
| 相手アイコン誤同定 (ギルガルド↔サーフゴー等) / 選出でslot0が埋まらない | spriteid の精度。選出画面の相手枠が4/6しか取れないケースも | **未着手** (P2残: テンプレ/候補の改善) |
| 交代メニューが scene=watch (opus指摘) | レイアウトが様子見画面とほぼ同一で、watch抽出は自分中心設計+帰属ガードのため実害は側柱読みの頻度のみ (上記で対処)。独自シーン化は見送り | 設計判断として記録 |

前回 (16時) の修正群は実地で有効を確認: 2戦目 一致100%・欠陥0、
churn 6→1-2件、選出picked一致、ヒステリシス動作。

## 2026-08-18 追従テスト (テストA) の結果

2試合で早期終了 (想定より欠陥が多いため)。配信レイヤは健全:
助言あり 100% / 10秒以内 100% (最大5.6秒)。以下は発見した欠陥。
決定監査の実測とタイムラインは `logs/battles/battle_20260818_16*.jsonl`。

| # | 欠陥 | 実測 | 修正箇所の当たり |
|---|---|---|---|
| 1 | **瀕死後の強制交代で技しか提案しない** | turn4: ムクホーク瀕死後も助言が「ブレイブバード」のまま (194〜216秒) | `advisor/service.py advise`: 自分activeが瀕死なら交代候補のみ評価する分岐が無い |
| 2 | **2ステータス同時変化の片方しか拾えない** (捨て台詞/瞑想等) | 捨て台詞で `boost_player_spa_-1` のみ発火 (atk側欠落) | `vision/events.py _parse_rank_change`: 最初に見つけた1ステータスで break している。技名からの補完 (partingshot等) も単一stat |
| 3 | **確定後に推奨順が入れ替わる (churn)** | 1戦目: 9決定中6件で決定中に反転。押下0.2秒後の反転も実測 | `advisor/service.py`: 状態更新ごとに再計算し無条件で差し替え。スコア差が閾値未満なら前回bestを維持するヒステリシスが無い |
| 4 | **先後の実測を無視して先制技を選ばない** (相手が常に先制でこのターン倒されるのに) | ユーザー観察 (2戦) | `advisor/engine.py`: 被ダメ≧残りHPかつ相手が先の局面で「行動できない前提」の期待値0化がされず、通常技の期待ダメージがそのまま採点される |
| 5 | **設置技持ちを先発に置かない** | ユーザー観察 | `advisor/selection.py:394`: 先発=相性平均スコア最大のみ。設置役の先発ボーナスが無い |
| 6 | 選出画面の picked フラグが1体目までしか取れない (一度Trueに立った後Falseへ戻る) | 1戦目の選出ログ | `vision/extractors.py` の選出済み判定。決定監査は出場種族へのフォールバックで回避済み |
| 7 | **戦闘不能の検知漏れでHPが固着** (倒れた後も非ゼロ値のまま複数フレーム持続) | 視覚監査: ミミッキュ「たおれた」後も43%、サザンドラ 0/169 表示なのに100%主張 | ひんし→HP0の状態反映。#1 (強制交代で技提案) の根と同族の可能性 |
| 8 | **HP変動・種族の誤帰属** | 視覚監査: HUDはフラエッテなのにオーロングのHP変動として記録 (深刻度:高)。2戦目で自分側に4種族観測 (3体選出なのに) も同族の疑い | HP変動の帰属ロジック (`vision/extractors.py` / `events._target_mon`)。8/11の「自分の行動が複数記録」と同族 |
| 9 | **シーン分類のfieldフォールバック誤判定** (メインメニュー・選出画面を field と分類) | 視覚監査: 選出画面が field 扱いになり `item_player_marble` の誤イベントまで発火 | `vision/scenes.py classify`: どれにも該当しない場合の既定が field。「不明」シーンを設けるか判定条件の追加 |
| 10 | **相手activeの長時間未特定** (HUDに相手が明示されているのに active=None が7フレーム) | opus視覚監査 (sonnetは見逃し) | 相手activeの紐付け (`link_active_to_party` / HUD名の解決)。active=None の間は助言が相手不明のまま劣化する |
| 11 | **自分activeがひんし個体を指したまま** (ハイライトはミミッキュ/サザンドラなのに active=ペロリーム0%) | opus視覚監査 ×2フレーム | 様子見/選出画面での自分activeインデックス更新。#7 (誤帰属) と同族 |

### 修正状況 (2026-08-18 深夜、全件回帰テスト付き)

- **修正済み**: #1 (ひんしactiveは交代のみ提案) / #2 (行動前に倒される技を
  RLブレンド後に0.25へ割引) / #3 (ヒステリシス margin=6.0) /
  #4 (設置持ちの先発ボーナス) / #5 (全ステータスへ適用) /
  #6 (連続ゼロ読み3回でひんし確定) / #8 (カーソル行=判定不能+解除は
  連続2フレームで確定) / #9の選出画面分 (先頭2行max+ライム加算。
  実フレームで field→selection 復帰を確認) /
  **#10 (ロスター事前分布つき再解決 0.75 + アイコン照合候補を判明済み
  スロットへ拡張。実フレームで active=None→オーロンゲ復旧を確認)**
- **非欠陥と確定**: #11 — ひんし後・交代選択中は「直前のひんし個体が
  active」が忠実な状態 (場に誰もいない)。ログのトレースで交代メッセージ
  発火と同時に active が即更新されることを確認。監査の指摘はカーソル行
  との比較で、実害 (ひんし個体の技助言) は #1 の修正で解消済み
- **部分修正で受容**: #7 — 回復方向のブロックに加え、#10 の名前解決強化で
  照合失敗自体が減る。減少方向の小変化は削りダメージ追跡のため許容を維持
  (次回テストで誤帰属が再発したら名前検証TTLを検討)
- **見送り (実測で実害なし)**: #9のメニュー→field誤分類 — 対象フレームを
  パイプラインに通して events=[]・状態変化なしを確認 (表示上の分類のみ)。
  判別材料が1フレームでは不足のため過学習を避けて見送り
- 視覚監査は同一30フレームの実測比較で opus に切替済み
  (sonnetは #10/#11 系統の状態欠落を見逃した)

監査ツール側の較正 (助言反転を偽不一致にしない / 選出をグラウンド
トゥルース比較にする) は同日実施済み。視覚監査の全文は
`logs/audit_reports/` の当日分を参照。

今回の更新には「静止画・モック・ハンドラ直呼びでは検証済みだが、
実映像+実操作では未検証」の機能が含まれる。次回の接続テストで
以下を上から順に確認する。

## 0. 前提と起動

- [ ] `bash scripts/start_connection_test.sh` を実行する
      (Showdown確保 → 最新コードでアドバイザー+フロント起動 →
      セッション開始マーカー記録、まで一括。フレーム保存も有効)
      ※ 学習ループは止めない (実測で影響なしと確認。tools/bench_pipeline)
- [ ] ブラウザで表示されたURL (3000) を開き「バックエンド: 接続中」表示

## 1. 追従テスト (今回のメイン): 全操作をアドバイス通りに行う

1決定 = 1テストケースとして扱う。人間の判断を混ぜないことで、
「助言→行動→結果」の因果が閉じ、欠陥が行動として顕在化する。

### 実施手順

- [ ] すべての決定 (選出3体・先発・技・交代・メガ) をアドバイスの
      最上位に従う
- [ ] **従えない場合の既定動作**: タイマー残り10秒までに助言が出ない /
      表示が曖昧 / 実行不能なら、自分の判断で打つ。
      **手動でのメモは不要** (逸脱はログから自動検出される)
- [ ] カジュアルマッチで開始し、動作が確認できたらランクマへ
      (相手分布と型予測の校正はランクマの方が本物)
- [ ] 各対戦後またはセッション末に決定監査を回す:
      `python -m tools.decision_audit` (今日の全対戦。`--last N` / `--json` あり)

### 合格基準 (集計行で確認)

- [ ] **助言あり 100%** — 欠け = 配信欠落 or シーン/行動イベントの取り逃し
- [ ] **一致 100%** — 不一致 = 実行ミス / 行動イベントの誤帰属 /
      表示の曖昧さ のどれか (テストAでは全て不具合)
- [ ] **時間内 100%** (既定10秒) — 遅延 = パイプライン詰まり
- [ ] `⚠ [大失点]` は不具合ではなく**妥当性の監査対象** (アドバイス品質の
      分岐点候補)。ログを添えて後で個別に検討する
- [ ] 選出行: メンバー一致 / 先発一致

⚠ 既知の参考値: 8/11の自由プレイログ3戦に対する監査で「同一ターンに
自分の行動が複数記録される」ケースを検出済み (行動イベントの誤帰属疑い)。
追従テストで再現したら decision_audit の ⚠ 行とフレーム保存から追跡する。

## A. もっと見る画面の自パーティ自動登録 (最重要・新機能)

選出画面または交代画面で、各ポケモンにカーソル→「もっと見る」を開く。

- [ ] **能力タブ**を開く → イベントログに `my_team更新: <種族> (能力タブ)` が出る
- [ ] **ステータスタブ**へ切り替える → `my_team更新: <種族> <性格> ...` が出る
- [ ] 6体ぶん繰り返し、`config/my_team.json` に 技/特性/持ち物/能力ポイント/性格 が
      入っていることを確認 (`python -m tools.check_battle_log` ではなくファイル直接確認で良い)
- [ ] わざと素早くタブを切り替えた場合に**誤った値が保存されない**こと
      (理論値照合があるので「保存されない」が正常。`もっと見る不一致` ログが出る)
- [ ] 登録後の対戦で、ダメージ計算・素早さ比較が登録した型 (実数値) で
      行われている感触を確認 (アドバイスの被ダメ%が実測と合うか)

⚠ 既知の注意: スクショ検証は1334x750のOBS映像で実施。解像度や
圧縮率が大きく違う環境では `python -m tools.check_look_more <frame>` で
読取値を確認する。

## B. 対戦中の回帰確認 (watch抽出を再構成したため)

- [ ] 対戦中の「様子を見る」(相手詳細) が従来どおり動く
      (相手のタイプ/技/持ち物が状態に入る、誤帰属しない)
- [ ] 様子見画面の左列 (自分のHP実数値) / 右列 (相手HP%) の読み取り
- [ ] 選出画面のアドバイス、対戦中のアドバイス配信が従来どおり
- [ ] メガシンカ・交代・HP変化の追跡に退行がない

## C. 分析・コーチングパネル (フロント新設)

対戦していない時間帯に:

- [ ] 「敗因分析」→ 数秒で勝敗/負け寄与/選出別勝率/ローカルメタが表示される
- [ ] 「環境ダイジェスト」→ 使用率上位と自分の実測メタの比較が表示される
- [ ] 「直近対戦レビュー」→ 最新の勝敗確定対戦の一致率と分岐点が表示される
- [ ] 「プレイブック生成」→ 進捗が逐次流れ、1〜2分で相手構築別チャートが表示される
- [ ] 「パーティ改善案」→ 固定枠 (例: ミミッキュ) と変更上限を指定して実行し、
      固定枠が維持された改善候補が出る
- [ ] 実行中は各ボタンが無効化され、完了で復帰する

対戦中に:

- [ ] 「プレイブック生成」を押す → 「対戦中のため実行できません」が返り、
      フレーム解析が詰まらない (アドバイス遅延が出ない)

## D. 人間 vs AI 対戦 (human_battle)

- [ ] `python -m tools.export_my_team_showdown` の出力にエラーがない
      (もっと見る登録後は6体とも出力されるはず)
- [ ] `python -m tools.human_battle --name <名前> --opponent benchmark` を起動
- [ ] ブラウザで `https://play.pokemonshowdown.com/~~localhost:8100/` を開き
      同名でログイン → チャレンジが届く
- [ ] チームビルダーに export の出力を Import してフォーマット
      `[Gen 9] Champions BSS Reg MB` で受諾 → 対戦が最後まで進行する
- [ ] 終了後 `logs/human_battles.jsonl` に結果が記録される
- [ ] (余裕があれば) `--opponent model` / `--opponent search` も1戦ずつ

## E. テスト後の確認 (一括監査込み)

- [ ] `bash scripts/end_connection_test.sh` を実行する
      (アドバイザー/フロント停止 → 学習ループ確認 → フレーム統計 →
      セッションのサマリー → **セッション一括監査**、まで一括。
      監査はsonnet 1回・数分。省く場合は `--no-audit`)
- [ ] フレーム統計の取りこぼし率を確認する (改善前の基準は62%・3.8fps。
      これを下回っていれば取りこぼし対策が効いている)
- [ ] 表示されたサマリーに今回の対戦が反映されている
- [ ] `logs/audit_reports/session_*.md` の乖離一覧を確認し、
      本物の抽出ミスがあれば控えておく (修正候補)。⚠矛盾候補は
      機械検出済みの疑い箇所なので優先的に見る

## 切り分けに使うコマンド

| 症状 | コマンド |
|---|---|
| もっと見るの読取がおかしい | `python -m tools.check_look_more <frame...>` |
| シーン分類がおかしい | `python -m tools.check_scene_sweep 50` |
| 抽出全般の突き合わせ | `python -m tools.audit_subtask --battle <log>` |
| チーム評価が不安定 | `python -m tools.check_team_eval` |
| human_battleが繋がらない | `python -m tools.check_human_battle` (AI対AIで疎通確認) |
