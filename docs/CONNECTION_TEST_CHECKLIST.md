# 接続テスト チェックリスト (2026-07-24 更新分)

## 2026-08-18 追従テスト (テストA) の結果

2試合で早期終了 (想定より欠陥が多いため)。配信レイヤは健全:
助言あり 100% / 10秒以内 100% (最大5.6秒)。以下は発見した欠陥。
決定監査の実測とタイムラインは `logs/battles/battle_20260818_16*.jsonl`。

| # | 欠陥 | 実測 | 修正箇所の当たり |
|---|---|---|---|
| 1 | **瀕死後の強制交代で技しか提案しない** | turn4: ムクホーク瀕死後も助言が「ブレイブバード」のまま (194〜216秒) | `advisor/service.py advise`: 自分activeが瀕死なら交代候補のみ評価する分岐が無い |
| 2 | **2ステータス同時変化の片方しか拾えない** (捨て台詞/瞑想等) | 捨て台詞で `boost_player_spa_-1` のみ発火 (atk側欠落) | `vision/events.py _parse_rank_change`: 最初に見つけた1ステータスで break している。技名からの補完 (partingshot等) も単一stat |
| 3 | **確定後に推奨順が入れ替わる (churn)** | 1戦目: 9決定中6件で決定中に反転。押下0.2秒後の反転も実測 | `advisor/service.py`: 状態更新ごとに再計算し無条件で差し替え。スコア差が閾値未満なら前回bestを維持するヒステリシスが無い |
| 4 | **先後の実測を無視して先制技を選ばない** (相手が常に先制でこのターン倒されるのに) | ユーザー観察 (2戦) | `advisor/engine.py`: 被ダメ≧残りHPかつ相手が先の局面で「行動できない前提」の期待値0化がされず、通常技の期待ダメージがそのまま採点される |
| 5 | **設置技持ちを先発に置かない** | ユーザー観察 | `advisor/selection.py:394`: 先発=相性平均スコア最大のみ。設置役の先発ボーナスが無い |
| 6 | 選出画面の picked フラグが1体目までしか取れない (一度Trueに立った後Falseへ戻る) | 1戦目の選出ログ | `vision/extractors.py` の選出済み判定。決定監査は出場種族へのフォールバックで回避済み |

監査ツール側の較正 (助言反転を偽不一致にしない / 選出をグラウンド
トゥルース比較にする) は同日実施済み。#1〜#5 の修正はいずれも
回帰テストとのセットで行うこと。

今回の更新には「静止画・モック・ハンドラ直呼びでは検証済みだが、
実映像+実操作では未検証」の機能が含まれる。次回の接続テストで
以下を上から順に確認する。

## 0. 前提と起動

- [ ] `bash scripts/start_connection_test.sh` を実行する
      (Showdown確保 → 最新コードでアドバイザー+フロント起動 →
      セッション開始マーカー記録、まで一括。フレーム保存も有効)
      ※ 学習ループは止めない (実測で影響なしと確認。tools/bench_pipeline)
- [ ] ブラウザで表示されたURL (3000) を開き「バックエンド: 接続中」表示

## 1. 追従テスト (今回のメイン): 全操作をアドバイス通りに行う

1決定 = 1テストケースとして扱う。人間の判断を混ぜないことで、
「助言→行動→結果」の因果が閉じ、欠陥が行動として顕在化する。

### 実施手順

- [ ] すべての決定 (選出3体・先発・技・交代・メガ) をアドバイスの
      最上位に従う
- [ ] **従えない場合の既定動作**: タイマー残り10秒までに助言が出ない /
      表示が曖昧 / 実行不能なら、自分の判断で打つ。
      **手動でのメモは不要** (逸脱はログから自動検出される)
- [ ] カジュアルマッチで開始し、動作が確認できたらランクマへ
      (相手分布と型予測の校正はランクマの方が本物)
- [ ] 各対戦後またはセッション末に決定監査を回す:
      `python -m tools.decision_audit` (今日の全対戦。`--last N` / `--json` あり)

### 合格基準 (集計行で確認)

- [ ] **助言あり 100%** — 欠け = 配信欠落 or シーン/行動イベントの取り逃し
- [ ] **一致 100%** — 不一致 = 実行ミス / 行動イベントの誤帰属 /
      表示の曖昧さ のどれか (テストAでは全て不具合)
- [ ] **時間内 100%** (既定10秒) — 遅延 = パイプライン詰まり
- [ ] `⚠ [大失点]` は不具合ではなく**妥当性の監査対象** (アドバイス品質の
      分岐点候補)。ログを添えて後で個別に検討する
- [ ] 選出行: メンバー一致 / 先発一致

⚠ 既知の参考値: 8/11の自由プレイログ3戦に対する監査で「同一ターンに
自分の行動が複数記録される」ケースを検出済み (行動イベントの誤帰属疑い)。
追従テストで再現したら decision_audit の ⚠ 行とフレーム保存から追跡する。

## A. もっと見る画面の自パーティ自動登録 (最重要・新機能)

選出画面または交代画面で、各ポケモンにカーソル→「もっと見る」を開く。

- [ ] **能力タブ**を開く → イベントログに `my_team更新: <種族> (能力タブ)` が出る
- [ ] **ステータスタブ**へ切り替える → `my_team更新: <種族> <性格> ...` が出る
- [ ] 6体ぶん繰り返し、`config/my_team.json` に 技/特性/持ち物/能力ポイント/性格 が
      入っていることを確認 (`python -m tools.check_battle_log` ではなくファイル直接確認で良い)
- [ ] わざと素早くタブを切り替えた場合に**誤った値が保存されない**こと
      (理論値照合があるので「保存されない」が正常。`もっと見る不一致` ログが出る)
- [ ] 登録後の対戦で、ダメージ計算・素早さ比較が登録した型 (実数値) で
      行われている感触を確認 (アドバイスの被ダメ%が実測と合うか)

⚠ 既知の注意: スクショ検証は1334x750のOBS映像で実施。解像度や
圧縮率が大きく違う環境では `python -m tools.check_look_more <frame>` で
読取値を確認する。

## B. 対戦中の回帰確認 (watch抽出を再構成したため)

- [ ] 対戦中の「様子を見る」(相手詳細) が従来どおり動く
      (相手のタイプ/技/持ち物が状態に入る、誤帰属しない)
- [ ] 様子見画面の左列 (自分のHP実数値) / 右列 (相手HP%) の読み取り
- [ ] 選出画面のアドバイス、対戦中のアドバイス配信が従来どおり
- [ ] メガシンカ・交代・HP変化の追跡に退行がない

## C. 分析・コーチングパネル (フロント新設)

対戦していない時間帯に:

- [ ] 「敗因分析」→ 数秒で勝敗/負け寄与/選出別勝率/ローカルメタが表示される
- [ ] 「環境ダイジェスト」→ 使用率上位と自分の実測メタの比較が表示される
- [ ] 「直近対戦レビュー」→ 最新の勝敗確定対戦の一致率と分岐点が表示される
- [ ] 「プレイブック生成」→ 進捗が逐次流れ、1〜2分で相手構築別チャートが表示される
- [ ] 「パーティ改善案」→ 固定枠 (例: ミミッキュ) と変更上限を指定して実行し、
      固定枠が維持された改善候補が出る
- [ ] 実行中は各ボタンが無効化され、完了で復帰する

対戦中に:

- [ ] 「プレイブック生成」を押す → 「対戦中のため実行できません」が返り、
      フレーム解析が詰まらない (アドバイス遅延が出ない)

## D. 人間 vs AI 対戦 (human_battle)

- [ ] `python -m tools.export_my_team_showdown` の出力にエラーがない
      (もっと見る登録後は6体とも出力されるはず)
- [ ] `python -m tools.human_battle --name <名前> --opponent benchmark` を起動
- [ ] ブラウザで `https://play.pokemonshowdown.com/~~localhost:8100/` を開き
      同名でログイン → チャレンジが届く
- [ ] チームビルダーに export の出力を Import してフォーマット
      `[Gen 9] Champions BSS Reg MB` で受諾 → 対戦が最後まで進行する
- [ ] 終了後 `logs/human_battles.jsonl` に結果が記録される
- [ ] (余裕があれば) `--opponent model` / `--opponent search` も1戦ずつ

## E. テスト後の確認 (一括監査込み)

- [ ] `bash scripts/end_connection_test.sh` を実行する
      (アドバイザー/フロント停止 → 学習ループ確認 → フレーム統計 →
      セッションのサマリー → **セッション一括監査**、まで一括。
      監査はsonnet 1回・数分。省く場合は `--no-audit`)
- [ ] フレーム統計の取りこぼし率を確認する (改善前の基準は62%・3.8fps。
      これを下回っていれば取りこぼし対策が効いている)
- [ ] 表示されたサマリーに今回の対戦が反映されている
- [ ] `logs/audit_reports/session_*.md` の乖離一覧を確認し、
      本物の抽出ミスがあれば控えておく (修正候補)。⚠矛盾候補は
      機械検出済みの疑い箇所なので優先的に見る

## 切り分けに使うコマンド

| 症状 | コマンド |
|---|---|
| もっと見るの読取がおかしい | `python -m tools.check_look_more <frame...>` |
| シーン分類がおかしい | `python -m tools.check_scene_sweep 50` |
| 抽出全般の突き合わせ | `python -m tools.audit_subtask --battle <log>` |
| チーム評価が不安定 | `python -m tools.check_team_eval` |
| human_battleが繋がらない | `python -m tools.check_human_battle` (AI対AIで疎通確認) |
