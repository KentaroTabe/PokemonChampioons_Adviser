# 信念つき探索への設計転換 — 外部フィードバックの事実確認と実験計画 (2026-09-04)

2026-09-04 に得た外部フィードバック (Laplace / Showdown Copilot / Metamon 等との
比較) を、**コードの実態と突き合わせて**採否を決め、実験を事前登録の形に落とす。
本書は計画であり、実装は未着手 (§6)。

---

## 1. 事実確認 — フィードバックの前提 vs 実装の実態

| フィードバックの前提 | 実態 (確認箇所) | 帰結 |
|---|---|---|
| 型予測は「一番ありそうな型」を1つ決める | **仮説分布は既にある**: `advisor/ev_infer.SpreadEstimator` が性格×配分×持ち物の仮説集合を対数重みで保持し、整合性・行動順 (×4/×0.1)・被ダメージで更新する。ただし意思決定側は `best_for()` の**点推定だけ**を探索へ渡す (`advisor/engine.py:301`)。技は `SetPredictor` の周辺分布 | 提案①は「分布を探索に流す」だけで着手できる。最小コストで最大の提案 |
| 探索/期待値核と PPO を最後にブレンドしている | 最終ブレンド (`rl_bridge`) に加え、**RL価値関数の葉評価は既に配線済み** (`engine.py` の `leaf_fn`、モデルがあれば常時有効)。ただし探索プレイヤー (`search_expert`) での実測は価値ブレンドが**逆効果** (0.44→0.34、報酬スケールの較正不良、2026-07-27)。アドバイザー経路の葉評価の効果は**未測定** | 「RLを探索の中へ」は既に半分入っており、効かせるには**較正**が先 (P6-a) |
| 次候補は v7 set encoder | **8/14 無益性棄却、8/17 v7観測+MLP も棄却** (−0.130、95%CI −0.144〜−0.116、docs/RL_V7_SET_ENCODER_DESIGN.md)。総括文書が「次候補」と誤記していた (本日訂正) | 提案④はそのままでは再挑戦しない。役割 (探索の事前分布/相手モデル/価値) を変えるなら別の設計として事前登録 |
| 探索が核で強く、NN は補正役 | 純探索 (同時手番の利得行列+2手読み、ロール3群) 単体は bench **0.41〜0.44**、PPO方策は **0.56〜0.62** (`search_expert` docstring の実測)。Laplace 型の深い MCTS は無い | 本プロジェクトでは核は現状 PPO の方が強い。探索内へRLを入れる価値は、探索自体の深さ/幅の強化と一体でないと出にくい |
| 相手最善応答 (minimax) に寄りがち | 既に **期待値 (相手行動分布で加重) と保証値 (minimax) の動的ブレンド** (`RISK_WEIGHT` 0.4、優勢時 0.6) と択リスク表示がある (`advisor/search.py`)。CVaR・仮説間分散は無い | 提案⑤は拡張として妥当 (P7 の副産物で仮説間分散が取れる) |
| 推奨は1位だけ | 上位3行動を 期待/保証/最悪応手つきで出している。条件付き推奨・安定度は無い | 安定度と「型依存」の表示は P7 の副産物 |
| churn 対策はヒステリシスのみ | 正しい (`advisor/service.py` margin 6.0) | 安定度 P(argmax不変) で一段進める |
| h2h 0.566 がブレンドの性能 | **誤り**。0.566 は PPO方策 current の学習評価 (h2h vs 3_best)。**アドバイザー (探索+RL) 自体の対戦強度は未測定** | 測定基盤の欠落。advisor-as-player の評価経路が要る (§4) |
| LLM をターン内に置かない | 既にそう (LLM は監査 `tools/audit_subtask` のみ) | 変更なし |
| ノイズ注入シム評価は予定段階 | 決定再生ハーネス (8摂動・推奨反転率) は実装済み・オフライン。シム内注入は未 | P8 の測定に既存ハーネスを使う |
| Laplace 2231 Elo 等の外部数値 | 未確認 (本書では根拠に使わない) | — |

結論: フィードバックの**方向 (belief → 多世界探索 → 集約 → 助言/エージェント) には同意**する。
ただし着手順は実態に合わせて **P7 (belief を探索へ) → P6-a (価値較正) → P6-b (相手行動の事前分布) → P8 (センサ不確実性)** とし、ネットワーク変更は役割が決まるまで行わない。

## 2. 目標像

```
観測 ─ 確率的状態 ─ 相手 belief (K仮説) ─┬─ 世界1 ─ 探索 ┐
                                          ├─ 世界k ─ 探索 ├─ 集約 (期待値/保証値/仮説間分散/安定度) ─ 助言
                                          └─ 世界K ─ 探索 ┘
                                                 ↑ RL: 相手行動の事前分布 / 較正済み価値 (葉)
```

同時手番・確率的結果・多世界を扱う探索を核に、RL は「探索を助ける事前分布と価値」として使う。
アドバイザーとエージェントは最後の1層 (推奨+安定度+仮定 vs 行動サンプル) だけを変える。

## 3. 実験の事前登録ドラフト (着手前に training_changes.json へ登録する)

すべて固定軸 (META_PIN=24 / POOL_PIN) で測り、主要指標は「同一窓の候補−基準の差」。

### P7 — belief-weighted search (最初に着手)
- **変更**: `engine` の `best_for()` を、`get_tracker()` の重み上位 K 仮説 (K=8、重み正規化) に置き換え、
  仮説ごとに同じ探索を実行し、行動ごとの期待値を重み平均する。
  副産物: 安定度 = Σ_k w_k·1[argmax_k = argmax]、仮説間分散、「仮説 n/K で安全」の説明文。
- **測定1 (対戦強度、シム)**: `search_expert` に同じ belief 経路を通し、`tools/check_search_expert` で
  点推定 vs belief を各300戦・同一相手列 (opp_seed 固定)。**ゲート**: 差 ≥ +0.05 かつ 95%CI下限 > 0
  (探索プレイヤー単体の基準 0.41〜0.44 を同日に再測する)。
- **測定2 (助言の反転率、オフライン)**: `scripts/advice_replay.sh` の8摂動で点推定 vs belief の反転率を比較。
  特に「相手判明技消失 23.9%」「相手持ち物消失」が下がるか。
- **レイテンシ**: p50/p95 を同時計測 (探索コストは K 倍)。上限は実測で決める (救出バッファの
  フレーム処理と競合しない範囲。目安 p95 < 1.0s)。

### P6 — RL統合方式
- **P6-a 価値較正**: 現行の葉評価 (常時有効) を with/without で測る (`check_search_expert --no-value` 相当を
  アドバイザー経路でも)。逆効果なら bench 勝率への較正 (Platt) を入れるか無効化する。
- **P6-b 相手行動の事前分布**: `opp_move_pool` の使用率重みを、自己対戦方策 (EMA) が相手の立場で出す
  行動確率と混合 (λ を事前登録)。測定: `search_expert` vs bench と h2h。
  実装設計: 探索は相手の行動候補 `_opp_actions` に確率 (技 0.85×使用率重み / 交代 残り) を
  与えている。RL側は `rl_bridge.value_of_sim` が SimSide から観測を組む経路を持つので、
  同じ経路で「相手を自分側に置いた」観測を作り、方策の行動分布 (技4+交代) を得る
  (`policy_of_sim`)。混合: p = (1−λ)·p_usage + λ·p_policy、λ は 0.5 で事前登録、
  相手の未判明技はプール技へ均等配分。腕: λ=0 (現行) vs λ=0.5、300戦・同一相手列。
  K と葉評価は P7/P6-a の判定結果の構成で固定して測る。アドバイザー経路では
  `policy_hint` に player/opponent を入れ替えた状態辞書を渡す形で同じ混合を行う。
- **P6-c 自分側の事前分布**: 探索候補の初期順位/剪定に方策確率。2手読みでは候補が少なく効果は薄い見込みで、
  深さ拡張とセットで検討。

### P8 — sensor-aware search
- 方針: 状態に信頼度を持たせ、**反転率の高いフィールドだけ分岐**する (decision-aware expansion)。
  最新の決定再生 (9/4、36決定点) では **自分HP固着 52.9%** が突出しているため、第一弾は
  「自分アクティブのHP」だけを対象にする。
- 実装 (2026-09-04): `search.sensor_worlds(me, q, delta)` — 確率 q で「表示より delta 低いHP」の
  世界を持ち、P7 の相手型世界との積を `aggregate_worlds` で統合する。エンジン側は
  `SENSOR_Q_DEFAULT` / `SENSOR_Q_UNCERTAIN` (hp_uncertain が立った個体) / `SENSOR_HP_DELTA` で
  制御し、既定 0 (判定まで助言経路は不変)。状態側に HP の鮮度時刻は無く、あるのは
  `hp_uncertain` フラグ (交代取り逃し時) のみ — 鮮度時刻の追加は P8 採用後の課題。
- 測定 (`scripts/p8_measure.sh`): シムで表示HPの固着ノイズを注入 (`displayed_hp`、確率 noise で
  前回表示のまま。対戦タグで種付けし腕間で近い雑音系列)。4腕・同一相手列・各300戦:
  noise下 unaware (q=0) / noise下 aware (q=noise) / 雑音なし unaware / 雑音なし aware。
- 判定: 雑音下の aware−unaware の95%CI下限 > 0 (頑健性の利得) かつ 雑音なしの
  aware−unaware の95%CI上限 > −0.03 (ヘッジの代償が小さい) で採用。K・葉評価・λ は
  P7/P6 の判定結果の構成で固定。決定再生の反転率 (自分HP固着) は副次指標。

### P9 — 探索値の推奨スコアへの統合 (助言側に P7/P6/P8 を効かせる前提)
- 発見 (9/4): 助言の推奨は「ダメージ計算ベースの採点 + RL方策ブレンド (25×確率)」で決まり、
  同時手番探索の結果は択評価の表示にしか使われていない。探索側の改良を助言に届けるには
  探索値をスコアに入れる必要がある。
- 設計: 探索結果の各行動 (技/交代) について `score += SEARCH_BLEND × (rec_a − max_rec)`
  (探索の推奨値 recommended ∈ 約[-1,1] の、最善からの差。最善は 0、劣る行動ほど減点)。
  SEARCH_BLEND=40 (RLブレンドの 25×確率 と同じ桁で、探索で −0.5 劣る行動が −20 点)。
  探索に現れない行動 (選べない技等) は変更なし。ヒステリシス等の後段は不変。
- 測定 (advisor-as-player、`tools/check_advisor_player`): 同一相手列・各300戦で
  (A) 現行 (統合なし、K=0) / (B) 統合あり K=0 / (C) 統合あり K=P7判定値 (採用時のみ)。
  葉評価は P6-a、λ は P6-b、q は P8 の判定結果で固定。副次指標: 決定再生の反転率、
  レイテンシ p50/p95、接続テストの churn。
- 採用条件: (B)−(A) または (C)−(A) が ≥ +0.05 かつ 95%CI下限 > 0。
  (C) が (B) に有意に勝てば多世界の助言側効果も確認できる。
- 前提: アドバイザー自身の基準強度 (A) をまず測る → **9/4 計測済み: 0.59** (300戦、p50 1,026ms / p95 1,893ms)。純探索 0.41/0.55 を上回り RL方策のベンチと同水準。

### 蒸留 (Metamon の代替)
- 探索教師→方策蒸留は `train/bc_pretrain --teacher search` として基盤がある。ただし教師 (探索) が
  方策より弱い間は無意味 (0.44 vs 0.56〜0.62)。P6/P7 で探索が方策を上回ってから。

## 4. 追加する評価指標

| 指標 | 現状 | 追加方法 |
|---|---|---|
| Cross-play matrix (current/best/EMA/探索プレイヤー/ヒューリスティクス) | 3性格_best との h2h のみ | `evaluate` の agents/benchmark を組み合わせる新ツール。自己対戦の循環 (じゃんけん) 検出用 |
| Advisor-as-player の対戦強度 | **無い** | アドバイザー経路 (belief+探索+RL) をプレイヤーとして bench/h2h で測る。P7 の測定1がその第一歩 |
| Recommendation stability / 仮説間分散 | 無い | P7 の副産物を advice payload に追加し、監査で集計 |
| Regret (探索最善との差) | 無い | 決定再生で算出 |
| Calibration (期待勝率 vs 実勝率) | 無い | シム評価で回収 |
| Noise robustness | 決定再生の反転率 (あり) | 継続 |
| Latency p50/p95 | **計測なし** | server の advice 計算時間をログへ (P7 と同時に) |
| Churn | 監査で集計 (あり) | 継続 |

## 4b. 検出力の指針 (9/4-5 の実測から)
- 同一設定の走行間差は 300戦で ≈0.03〜0.08 (K=0/葉評価ON: 0.547 / 0.510 / 0.51、雑音下 unaware:
  0.490 / 0.568)。300戦の SE ≈0.029 に対し、対応比較でも差の SE ≈0.036 で +0.05 ゲートは
  検出力不足。**以後の腕は既定 600戦 (SE≈0.026)、効果量 +0.03 を狙うなら 1,500戦**とし、
  前提となる「害」や「基準」も同日に対で取り直す (過去走の値を基準に流用しない)。
- 測定スクリプトの既定戦数を 600 に変更 (9/5)。

## 5. リスクと未確認事項
- K 仮説探索のレイテンシ (未実測)。
- 価値較正の失敗履歴 (2026-07-27)。較正なしで葉評価を強めない。
- 自己対戦方策を相手モデルにすると、人間の行動分布とズレる (λ で混合し、実対戦ログで検証)。
- 外部システムの数値は未確認。

## 6. 状態
**着手順 P7 → P6-a → P6-b → P8。** 各着手前に事前登録 (training_changes.json)。
v7 set encoder は棄却済み (再挑戦するなら役割を変えた別設計として登録)。

- 2026-09-04: P7 を事前登録 (19:40) し実装 (e3225d65)。測定1 (シム3腕) と測定2
  (決定再生) を実行中。中間所見: アドバイザー経路の探索は**点推定でも 1.2s**
  (RL価値の葉評価込み、テストの実局面)、K=8 で 4.8s。コストの主因は葉ごとの
  NN推論なので、アドバイザーへの多世界化の採否は P6-a (葉評価の較正/無効化) の
  結果と合わせて決める。シム (葉評価OFF) のレイテンシは測定1で計測
- **2026-09-04 20:10 重要な訂正**: 決定再生 (36決定点×8摂動) の反転率は点推定と多世界で
  **完全一致**した。原因は構造的で、アドバイザーの推奨スコア (`evaluate` の actions) は
  ダメージ計算ベースの採点 + RL方策ブレンドで決まり、**同時手番探索の結果 (`gtheory`) は
  「択評価」表示にしか使われていない** (engine.py: 生成と出力辞書への格納のみ)。
  したがって P7/P6/P8 の探索側の改良は、そのままでは助言の推奨を1つも変えない。
  シムの探索プレイヤーは探索で行動を決めるため測定1は有効。助言側に効かせるには
  「探索値のスコア統合」(P9、要事前登録) が必要で、その効果を測るには
  §4 で欠落としていた **advisor-as-player** (助言エンジンをプレイヤーとして対戦させる
  評価経路) が前提になる → 9/4 に実装着手。
- **2026-09-05 01:45 P10 判定: 棄却 (期待ダメージ推定は既定OFF)** — 雑音下 unaware 0.568 / estimate 0.535 (各600戦、−0.033±0.026)。同一設定の unaware が P8 で 0.490 だったため『雑音の害 −0.070』は走行間で再現せず、前提自体が揺れていた。実戦の固着はバー非表示中に集中するため、接続テストで頻度を実測してから再設計。
- **2026-09-05 (シーズン切替前の対策、ユーザー指示)**: 価値順1-3を実装し測定中 — (1) HP固着対策: 攻撃技イベントから期待ダメージを決定的に反映 (vision、既定ON) とシム再現 P10 (雑音0.3下 推定あり/なし 各600戦、ゲート +0.03)、(2) P9' 再登録 (各1,500戦、ゲート +0.03)、(3) P7' 観測更新つき多世界 (先後・被ダメ・持ち物、各300戦、ゲート +0.05)。
- **2026-09-05 00:25 P9 判定: 棄却 (SEARCH_BLEND=0 維持)** — A 0.593 / B 0.630 (各600戦)、+0.037±0.026、CI下限 −0.014。300戦 +0.057 と合わせ点推定は一貫して正・コストゼロだがゲート未達。再挑戦は最小効果量 +0.03・N≈1,500 で新規登録。
- **アーク総括**: 5本中 P6-a のみ維持、他4本は棄却。得たものは advisor-as-player (基準 0.59)、『探索は表示専用』の発見、雑音の害 −0.070、走行間差 ≈0.03〜0.04。
- **2026-09-04 23:30 P8 判定: 棄却 (q=0 維持)** — 雑音下 +0.017±0.040、雑音なし −0.033±0.037。雑音自体の害は −0.070 (認識側で固着を減らす投資の裏付け)。
- **2026-09-04 22:40 P7 判定: 棄却 (K=0 に復帰)** — 単一仮定 0.413 / MAP 0.367 / 多世界K=8 0.380、(c)−(a) −0.033±0.036。事前分布のみの多世界化は効かない。観測更新つき (実戦の SpreadEstimator) の効果は未検証 → 探索プレイヤーへ観測更新を配線してから別途登録。実装は残置
- **2026-09-04 21:30 P6-b 判定: 棄却 (λ=0 維持)** — λ0 0.510 / λ0.5 0.503、−0.007±0.036。同一設定の別走 (P6-a ON腕 0.547) との差 0.037 は走行間の揺れとして記録。
- **2026-09-04 20:25 P6-a 判定: 葉評価を維持 (有益)** — OFF 0.420 / ON 0.547、+0.127±0.040 (95%CI +0.048〜+0.205)、レイテンシ p50 256→342ms。以後の測定は葉評価ON。
- 2026-09-04 20:00: シム K=8・葉評価OFF の探索レイテンシは逐次 p50 3,130ms / p95 5,047ms
  (10戦)、世界の並列実行 (4ワーカー、`run_world_searches`) で p50 1,239ms / p95 2,029ms
  (3戦、測定2本と学習が同時に走る負荷下)。並列化は結果を変えない (テストで一致確認)。
  P6-b (149a8c7e) と P8 (6425d4ad) も実装済み・事前登録済みで、P7/P6-a の判定後に
  その構成で測る
- 2026-09-04 20:28: 葉評価ON + K=8 + 4ワーカー (leaf_ctx でワーカー側再構成) は p50 2,183ms / p95 4,937ms (3戦39決定、測定3本+学習と同時の負荷下、ワーカーのモデル読込を含む)。葉評価は並列でも主コストで、助言経路で K=8 を採る場合は空き状態での再計測と K の再検討 (4〜6) が要る
